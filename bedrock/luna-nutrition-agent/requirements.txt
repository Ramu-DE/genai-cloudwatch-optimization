# GenAI Pet Store Agents - Requirements
# Supports: Bella (Strands), Oliver (LangGraph), Luna (CrewAI), Max (Strands)

# Core AWS SDK
boto3>=1.40.0
botocore>=1.40.0

# Bedrock AgentCore Runtime
bedrock-agentcore>=0.1.7
bedrock-agentcore-starter-toolkit>=0.1.22

# HTTP Requests & Networking
requests>=2.32.0
urllib3>=2.0.0

# Data Processing
pydantic>=2.9.0
python-dateutil>=2.9.0

# Strands Framework (Bella, Luna)
strands-agents>=1.12.0
strands-agents-tools>=0.2.11

# LangGraph Framework (Max)
langgraph>=0.2.0
langchain-core==1.0.3
langchain-aws==1.0.0

# CrewAI Framework (Oliver)
crewai==1.3.0
#crewai-tools==1.3.0
opentelemetry-instrumentation-crewai>=0.47.5

websocket-client>=1.6.0
aws-xray-sdk>=2.12.0

# Core dependencies
langchain>=0.3.0

# HTTP Server
flask>=3.0.0
flask-cors>=4.0.0

# ADOT (AWS Distro for OpenTelemetry)
# Let aws-opentelemetry-distro manage all OTEL dependencies
aws-opentelemetry-distro>=0.12.0
